#include <stdio.h>
#include <log.h>
#include <data.h>
// I will have to use this file to create all kinds of tests,
// and use cases for the core of the project.
int main()
{
  const char* test = "";
  index_dir_structure(test);

  return 0;
}